// import { v4 as uuidv4 } from "uuid";

// export const createItems = (length = 100)  =>{
//    return Array.from({ length }).map(() => uuidv4());
// }

// export const loadMore = async (length = 100) =>
//   new Promise((res) => setTimeout(() => res(createItems(length)), 100));